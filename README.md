# compiler-simulation-ds-project
Queue Tabshair first collects each line of student code for processing. Linked List Aziz then stores the code's tokens in an ordered, connected sequence. Stack Hassaan is crucial for validating matching brackets and nested structures. Together, with Arrays for keywords, they form a mini compiler to detect basic syntax errors.
